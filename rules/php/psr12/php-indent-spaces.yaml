id: php-indent-spaces
title: 들여쓰기는 공백 4칸 (탭 금지)
severity: warn
superseded_by: [pint.json, .php-cs-fixer.php, .php-cs-fixer.dist.php]
applies_to:
  stacks: [php]
  files: ["**/*.php"]
  exclude: ["**/vendor/**"]
detect:
  when_line_added: '^\t'
message: |
  들여쓰기는 공백 4칸입니다. 탭을 쓰지 마세요.
tests:
  match:
    - "\tprivate string $name;"
  no_match:
    - "    private string $name;"
