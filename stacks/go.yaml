id: go
tags: [go]
detect:
  file: go.mod
  version_regex: '^go (\d+\.\d+)'
lint:
  - cmd: ["golangci-lint", "run", "{files}"]
    if_exists: golangci-lint
    files: ["**/*.go"]
  - cmd: ["go", "vet", "./..."]
    if_exists: go
    files: ["**/*.go"]
