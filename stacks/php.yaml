id: php
tags: [php]
detect:
  file: composer.json
lint:
  - cmd: ["./vendor/bin/php-cs-fixer", "fix", "--dry-run", "--diff", "{files}"]
    if_exists: vendor/bin/php-cs-fixer
    files: ["**/*.php"]
    parse: diff
