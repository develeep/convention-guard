id: js
tags: [js]
detect:
  file: package.json
lint:
  - cmd: ["npx", "--no-install", "eslint", "--max-warnings", "0", "{files}"]
    if_exists: node_modules/.bin/eslint
  - cmd: ["npx", "--no-install", "biome", "check", "{files}"]
    if_exists: node_modules/.bin/biome
